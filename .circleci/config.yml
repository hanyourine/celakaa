version: 2.1
jobs:
    trial:
      docker:
            - image: circleci/phyton@1.5.0
      parallelism: 16
      steps:
        - run:
            name: Install Dependencies
            command: |
              lscpu
              echo Hello,subscribe it

workflows:
    version: 2.1
    build:
        jobs:
        - trial